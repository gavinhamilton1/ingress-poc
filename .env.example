# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# GitHub credentials for GitOps integration
# Token needs: repo (read/write), workflow (if using Actions)
GITOPS_GITHUB_TOKEN=ghp_your_token_here
GITOPS_GITHUB_USERNAME=your_github_username
