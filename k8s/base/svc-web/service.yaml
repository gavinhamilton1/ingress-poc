apiVersion: v1
kind: Service
metadata:
  name: svc-web
  labels:
    app: svc-web
    ingress.jpmc.com/component: data-plane
spec:
  selector:
    app: svc-web
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
  type: ClusterIP
