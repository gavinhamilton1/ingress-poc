apiVersion: v1
kind: Service
metadata:
  name: mock-psaas
  labels:
    app: mock-psaas
    ingress.jpmc.com/component: data-plane
spec:
  selector:
    app: mock-psaas
  ports:
    - port: 8012
      targetPort: 8012
      protocol: TCP
  type: ClusterIP
