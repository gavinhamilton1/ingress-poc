apiVersion: v1
kind: Service
metadata:
  name: opa
  labels:
    app: opa
    ingress.jpmc.com/component: control-plane
spec:
  selector:
    app: opa
  ports:
    - port: 8181
      targetPort: 8181
      protocol: TCP
  type: ClusterIP
