apiVersion: v1
kind: Service
metadata:
  name: console
  labels:
    app: console
    ingress.jpmc.com/component: control-plane
spec:
  selector:
    app: console
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
