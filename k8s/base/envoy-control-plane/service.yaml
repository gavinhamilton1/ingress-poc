apiVersion: v1
kind: Service
metadata:
  name: envoy-control-plane
  labels:
    app: envoy-control-plane
    ingress.jpmc.com/component: control-plane
spec:
  selector:
    app: envoy-control-plane
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
