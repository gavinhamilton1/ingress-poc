apiVersion: v1
kind: Service
metadata:
  name: ingress-operator
  labels:
    app: ingress-operator
    ingress.jpmc.com/component: control-plane
spec:
  selector:
    app: ingress-operator
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
