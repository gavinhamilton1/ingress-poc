apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
    ingress.jpmc.com/component: data-plane
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
